import { LazyMessagesContent } from '@/components/dashboard/teacher/lazy/LazyMessagesContent';

export const metadata = {
  title: 'Messages - Learno Teacher',
};

export default function TeacherMessagesPage() {
  return <LazyMessagesContent />;
}
