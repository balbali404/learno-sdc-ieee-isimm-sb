import { LazySettingsContent } from '@/components/dashboard/teacher/lazy/LazySettingsContent';

export const metadata = {
  title: 'Settings – Learno Teacher',
};

export default function SettingsPage() {
  return <LazySettingsContent />;
}
