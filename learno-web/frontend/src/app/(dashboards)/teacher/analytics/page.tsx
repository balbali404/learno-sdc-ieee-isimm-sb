import { LazySessionAnalyticsContent } from '@/components/dashboard/teacher/lazy/LazySessionAnalyticsContent';

export const metadata = {
  title: 'Session Analytics – Learno Teacher',
};

export default function SessionAnalyticsPage() {
  return <LazySessionAnalyticsContent />;
}
