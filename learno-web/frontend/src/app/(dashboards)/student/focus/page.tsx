import { LazyStudentFocusPage } from '@/components/dashboard/student/lazy/LazyStudentFocusPage';

export const metadata = {
  title: 'Focus Mode - Learno Student',
};

export default function StudentFocusRoutePage() {
  return <LazyStudentFocusPage />;
}
