import { LazyStudentQuizPage } from '@/components/dashboard/student/lazy/LazyStudentQuizPage';

export const metadata = {
  title: 'Quizzes - Learno Student',
};

export default function StudentQuizzesRoutePage() {
  return <LazyStudentQuizPage />;
}
